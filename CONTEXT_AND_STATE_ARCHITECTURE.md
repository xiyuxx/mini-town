# Mini-Town 状态与上下文架构改造方案

## 1. 文档目的

本方案用于统一规划后续改动，解决以下问题：

- 完整状态需要持续保存，但不应在每次 LLM 请求中完整展开。
- LLM 请求应根据当前任务、意图和情境激活相关信息。
- 世界状态、角色运行状态、长期记忆和关系需要有清晰的存储边界。
- LLM 返回结果可能过时，不能直接覆盖当前状态。
- 上下文裁剪不能破坏权限、硬约束、事实来源和行为连续性。

本方案只描述目标架构和迁移步骤，不代表已经完成实现。

## 2. 当前基线

当前项目主要组件：

- `SimulationEngine`：推进模拟时间、处理 tick 和广播状态。
- `Agent`：保存角色状态、状态机、日程、计划和心智状态。
- `MemoryStore`：SQLite 记忆存储和记忆检索。
- `RelationshipStore`：SQLite 关系存储。
- `FactLedger`：当前进程内的事实账本。
- `ResourceStore`：内存中的资源、实体和过程状态。
- `CognitivePlanner`：每日计划、生活计划和行动决策。
- `DialogueManager`：跨 tick 的对话状态机。
- `WorldPack` / `LOCATION_MAP`：声明式世界和地点定义。

当前主要问题集中在 `Agent._build_planning_context()`：

- 每次规划都传递完整 `mental_state`。
- 每次规划都传递全部地点 `known_locations`。
- 记忆虽然已经通过 `MemoryStore.retrieve()` 做了一定筛选，但筛选维度仍较粗。
- 不同任务共享近似相同的大上下文，而不是使用任务专用视图。
- 角色心智中的目标、意图、信念和计划主要在内存中，跨进程重启不能完整恢复。

当前默认天气变化概率已经从 `0.35` 调整为 `0.08`，位置为 `backend/config.py`。

## 3. 核心原则

### 3.1 完整状态与工作上下文分离

完整状态必须保存，工作上下文只是一份面向当前任务的只读视图。

```text
完整状态
  -> 情境判断
  -> 相关状态检索
  -> 任务专用上下文
  -> LLM 决策
  -> 校验和提交
```

不能因为某字段没有放进 prompt，就删除或覆盖完整状态。

### 3.2 代码负责约束，LLM 负责选择

代码必须负责：

- 时间和状态推进
- 权限检查
- 资源可用性
- 路径和可达性
- 固定承诺
- 紧急需求识别
- 事实来源和可见性
- 版本和过期检查
- 行动效果提交

LLM 负责：

- 在给定事实和可行候选中进行角色化选择
- 产生自然语言内容
- 在允许范围内更新角色的主观认知

不能依靠 prompt 要求 LLM 自己保证这些硬约束。

### 3.3 当前状态和历史依据分开

当前天气、当前位置、当前需求等即时状态可以只保存在内存中。

记忆、关系、长期信念、长期意图和需要跨时间追溯的事实应持久化。

如果即时状态导致了长期影响，只持久化影响结果和来源，不必把每一次即时状态变化都作为独立实体保存。

示例：

```text
当前天气为大雨        -> 内存
因为大雨导致迟到      -> 事实
角色以后减少雨天出行  -> 记忆或意图
```

### 3.4 读取上下文必须有时间边界

所有数据库检索都必须限制在当前模拟时间以前，不能把未来记忆、未来事实或尚未提交的结果提供给角色。

### 3.5 LLM 结果必须经过版本校验

LLM 请求期间世界可能继续推进。所有请求必须绑定 Agent、模拟时间和状态版本，过时结果不能直接执行。

## 4. 数据分层与所有权

### 4.1 静态世界定义

来源：`WorldPack`，可由 JSON 文件加载。

包含：

- 地点定义和访问规则
- 地图和道路
- 角色初始资料
- 日程和 routine block
- 资源初始定义
- 过程、设施和场景规则
- 天气转移规则

这些数据应该只维护一份权威副本。不同 Agent 得到的是经过权限和任务筛选的视图，而不是各自维护一份地点数据。

### 4.2 内存运行状态

建议由 `RuntimeState` 或现有 `SimulationEngine` 统一持有：

- 当前模拟日期和时间
- 当前天气
- 当前季节和节日状态
- 基础设施即时状态
- Agent 位置、移动状态和动作状态
- Agent 当前需求和即时情绪
- 当前资源数量和位置
- 当前活动任务
- 当前 tick 内的临时事件
- 当前对话会话

这些内容主要服务于当前运行，不一定需要每次写数据库。

### 4.3 持久行为状态

应该由专门 Store 持久化：

- 记忆
- 关系及关系锚点
- 对话记录
- 长期信念
- 长期意图
- 跨日目标
- 跨重启仍应有效的习惯
- 需要审计和重建的事实

如果当前阶段不支持进程重启恢复，可以先把 goals、intentions、beliefs 保留在内存，但必须明确这是临时方案，而不是最终的完整状态保存方案。

### 4.4 历史和审计记录

以下数据可作为历史记录单独保存：

- 重要行动事实
- 对话事实
- 资源变化事实
- 关键环境变化造成的结果
- LLM 请求元数据
- 上下文中选用了哪些记忆、事实和地点
- 被裁掉了哪些低优先级字段

不需要将所有每 5 分钟的天气状态都写入数据库，除非未来需要完整回放天气轨迹。

## 5. 建议的核心组件

### 5.1 `WorldQuery`

负责从唯一的世界定义和当前运行状态中查询任务相关的世界信息。

建议接口：

```python
class WorldQuery:
    def available_locations(
        self,
        agent,
        task_type: str,
        snapshot,
        intention=None,
        limit: int = 6,
    ) -> list[dict]:
        ...

    def visible_environment(
        self,
        agent,
        snapshot,
        target_id: str = "",
    ) -> dict:
        ...

    def current_constraints(self, agent, snapshot) -> list[dict]:
        ...
```

查询时负责：

- 访问权限过滤
- 地点存在性校验
- 当前距离和可达性
- 天气造成的移动代价
- 当前地点和候选地点的资源
- 任务所需能力
- 当前角色实际能看到的实体

`known_locations` 不再默认返回全部地点，而是返回与当前任务有关的候选地点。对于必须知道的地点，例如固定承诺的目标地点，应始终保留。

### 5.2 `CognitionStore`

负责长期目标、意图、信念和计划的保存与读取。

建议把当前 `MentalState` 分为两层：

```text
MentalState
  - runtime projection：当前进程使用的快速状态
  - durable records：需要跨时间或跨重启保留的认知记录
```

如果使用内存 projection，必须带有：

- `state_version`
- `updated_at`
- `source_ids`
- `status`

每次持久认知变化都应该能追溯到事实或行动结果。

### 5.3 `MemoryRetriever`

负责根据任务检索记忆，而不是让每个调用点自行拼接记忆。

建议接口：

```python
class MemoryRetriever:
    async def search(
        self,
        agent_id: str,
        task_type: str,
        query: str,
        now: int,
        participants: list[str] | None = None,
        location: str | None = None,
        limit: int = 5,
    ) -> list[dict]:
        ...
```

排序可以组合：

- 当前任务或意图相关性
- 未解决状态
- 重要性
- 时间近度
- 当前地点
- 参与者
- 语义相似度
- 与当前目标的关联

返回结果应带检索原因和来源 ID，便于调试和审计。

### 5.4 `RelationshipQuery`

负责返回与当前任务相关的关系信息：

- 当前对话对象的熟悉度、亲密度和信任度
- 最近互动摘要
- 共同记忆对应的关系锚点
- 与当前意图有关的关系信息

对话只需要返回参与者之间的关系，不应加载 Agent 与全镇所有人的关系网络。

### 5.5 `ContextService`

统一组合内存状态、WorldQuery、CognitionStore、MemoryRetriever 和 RelationshipQuery。

建议接口：

```python
class ContextService:
    async def build(
        self,
        agent,
        task_type: str,
        snapshot,
        intention_id: str | None = None,
    ) -> dict:
        ...
```

`Agent`、`CognitivePlanner` 和 `DialogueManager` 只声明 `task_type`，不再直接拼接完整心智状态和全部地点。

## 6. 情境判断和激活规则

上下文构建不能只围绕一个意图，需要先计算当前主动因素。

建议优先级：

```text
安全和权限约束
> 已到期或即将到期的固定承诺
> 紧急需求
> 当前正在执行的计划步骤
> 高重要性开放意图
> 当前地点和附近社交因素
> 习惯和弹性活动
```

### 6.1 必须始终提供的信息

以下信息一般是行动决策的最低上下文：

- 当前模拟时间
- 当前地点和 Agent 状态
- 当前需求
- 当前天气及其即时影响
- 当前固定承诺
- 当前计划步骤
- 当前硬约束
- 当前可执行状态

### 6.2 按需提供的信息

以下信息根据任务检索：

- 相关目标
- 相关意图
- 相关信念
- 近期行动结果
- 当前地点的实体和过程
- 候选地点
- 相关记忆
- 相关关系
- 共同事实

### 6.3 不应默认提供的信息

- 与当前任务无关的旧目标
- 全部地点目录
- 全部居民关系
- 全部历史记忆
- 几天前且已解决的行动结果
- 当前任务无法使用的资源详情
- 与当前角色无关的事实

## 7. 任务专用上下文

不同任务使用不同的上下文格式，不共享一个无限增长的大字典。

### 7.1 行动决策

```json
{
  "request": {
    "agent_id": "mei",
    "sim_timestamp": 1080,
    "state_version": 42
  },
  "situation": {},
  "hard_constraints": [],
  "active_motives": [],
  "current_plan_step": {},
  "candidate_locations": [],
  "visible_environment": {},
  "nearby_agents": [],
  "relevant_memories": [],
  "relevant_beliefs": [],
  "recent_outcomes": []
}
```

### 7.2 每日计划

只提供当天固定承诺、需求、重要开放意图、可用地点类别和角色长期方向，不传全部历史行动。

### 7.3 连续生活计划

提供当前计划触发原因、即将到来的承诺、当前目标、有限的可执行地点和资源，不重复传输与本次计划无关的完整环境目录。

### 7.4 自由时间候选选择

候选已经由代码生成和校验，LLM 只需要看到：

- 当前需求
- 今日关注点
- 习惯摘要
- 附近社交信息
- 最多 6 个候选

不能让 LLM 在这个阶段重新发明地点或动作。

### 7.5 对话

只提供：

- 说话者和参与者
- 当前可观察事实
- 当前对话记录
- 双方近期互动
- 共同记忆
- 双方关系
- 当前可引用事实

不加载角色全部行动规划上下文。

### 7.6 记忆形成

只提供：

- 已发生的结构化事实
- 当前情绪
- 当前相关目标
- 事件参与者
- 来源事实 ID

不得将完整世界状态传给记忆形成请求。

## 8. 地点和世界数据设计

地点定义只维护一份，建议不要为每个 Agent 复制完整目录。

调用前由 `WorldQuery` 生成 Agent 专属视图：

```json
[
  {
    "id": "cafe",
    "name": "阳光咖啡馆",
    "type": "cafe",
    "accessible": true,
    "distance": 8,
    "relevance": 0.78,
    "reasons": ["符合当前开放意图", "地点中存在可交互资源"]
  }
]
```

需要始终遵守：

- 私人地点不能因为出现在世界目录中就对所有角色可见。
- `accessible=false` 的地点不能作为可执行移动候选。
- LLM 只能引用返回的真实地点 ID。
- 当前任务所需的固定目标地点即使相关性低，也必须保留。
- 没有检索到某地点不能等同于世界中不存在该地点。

## 9. 记忆、关系和事实设计

### 9.1 记忆

记忆是持久行为依据，但不等于每次都全部加载。

检索结果需要区分：

```text
相关且确定的记忆
相关但不确定的记忆
没有检索到的内容
当前角色无权知道的内容
```

记忆内容应保留：

- `source_ids`
- `event_type`
- `participants`
- `sim_timestamp`
- `importance`
- `unresolved`
- `future_intention`
- `confidence`

### 9.2 关系

关系数据持久化在 `RelationshipStore`，当前对话只加载相关参与者之间的关系。

关系变化必须来自明确互动、共同经历或对话评估，不能由 LLM 随意修改。

### 9.3 事实

如果事实只用于当前进程审计，可以保留在内存的 `FactLedger`。

如果事实是信念、意图或长期记忆的来源，应持久化，或保证可以从持久历史重建。

所有 LLM 返回的事实引用必须经过：

- Agent 可见性检查
- 模拟时间检查
- 来源存在性检查
- 参与者和地点一致性检查

## 10. 请求和提交协议

每个异步 LLM 请求都应携带请求元数据：

```json
{
  "agent_id": "mei",
  "task_type": "action_decision",
  "sim_timestamp": 1080,
  "state_version": 42,
  "intention_id": "intent_xxx",
  "plan_id": "plan_xxx",
  "plan_step_id": "step_xxx"
}
```

LLM 返回后按以下顺序处理：

1. 解析结构化响应。
2. 检查当前 Agent 是否仍存在。
3. 检查请求对应的意图和计划步骤是否仍有效。
4. 检查当前状态版本是否允许提交。
5. 重新验证权限、路径、资源和行动可行性。
6. 如果请求已过期，则丢弃、标记为 stale，或重新构建上下文。
7. 通过统一提交流程修改内存状态和持久状态。
8. 写入事实、trace 和请求元数据。
9. 增加状态版本。

不能因为 LLM 返回的是合法 JSON，就直接执行旧决定。

## 11. 持久化策略

### 11.1 建议持久化的内容

- 记忆及其来源
- 关系及关系锚点
- 对话及消息
- 长期信念
- 未完成且跨日有效的意图
- 跨日目标
- 跨重启仍有效的习惯
- 关键事实或可重建事实的事件记录

### 11.2 可以只保存在内存的内容

- 当前天气
- 当前季节显示状态
- 当前 tick 的临时事件
- 当前移动路径
- 当前动作剩余 tick
- 当前连接和后台任务
- 当前对话执行中的临时索引

### 11.3 可选 checkpoint

如果未来需要应用重启后继续模拟，应定期保存：

- 当前模拟时间
- Agent 位置和需求
- 当前动作
- 当前计划步骤
- 资源运行状态
- 天气当前状态
- 状态版本

这属于断点恢复功能，不应和长期记忆持久化混为一谈。

## 12. 上下文预算与观测

上下文服务应该记录以下元数据，但不要把这些诊断信息默认交给 LLM：

- 任务类型
- 输入字符数和 token 估算
- 选中的记忆 ID
- 选中的事实 ID
- 选中的地点 ID
- 选中的目标和意图 ID
- 被裁剪的字段
- 状态版本
- 模拟时间
- LLM 请求耗时
- 是否发生重试或过期

建议设置任务级预算：

- 行动决策：优先保证硬约束和可行性信息
- 对话：优先保证当前对话、共同事实和关系
- 记忆形成：优先保证结构化事实和来源
- 反思：优先保证时间窗口内的重要经历

超出预算时，裁剪顺序应为：

```text
低相关旧记忆
> 无关地点
> 已解决的旧结果
> 低优先级习惯
> 次要描述
```

不能先裁掉权限、承诺、当前计划步骤或资源可行性信息。

## 13. 失败与退化策略

### 13.1 LLM 请求失败

- 不阻塞模拟时钟。
- 保留当前 Agent 状态。
- 记录 `decision_deferred` 或对应失败事件。
- 使用经过代码校验的确定性 fallback。
- 不凭空创造新的生活动作。

### 13.2 检索为空

- 显式传递“没有检索到相关信息”。
- 不让 LLM 将空结果理解为事实不存在。
- 对需要证据的行动要求先观察或查询。

### 13.3 返回结果过时

- 丢弃过时行动。
- 写入 stale 诊断信息。
- 根据当前状态重新排队，而不是执行旧行动。

### 13.4 上下文裁剪过度

- 保留硬约束。
- 增加 `omitted_sections` 诊断字段。
- 允许在关键任务中扩大预算或触发二次检索。

## 14. LLM 思考模式策略

当前配置默认使用 `chat` 模式，普通规划和对话调用已经显式关闭 thinking。

继续采用按任务划分的策略：

- 高频行动选择：`chat`
- 高频对话回复：`chat`
- 候选选择：`chat`
- 记忆编码：`chat`
- 低频反思：可以使用 `thinking`
- 高价值记忆的心理解释：可以使用 `thinking`
- 对话结束后的关系评估：可以使用 `thinking`

后续应根据实际耗时、质量和成本评估是否需要进一步关闭低频 thinking，而不是全局修改。

## 15. 建议的迁移顺序

### 阶段 1：只增加观测，不改变行为

- 增加 `DecisionSnapshot`。
- 记录当前上下文各字段大小。
- 记录每次请求的任务类型、耗时、状态版本和输入规模。
- 保留现有 prompt 内容作为对照组。

验收：现有后端测试全部通过，能获得各任务的真实输入统计。

### 阶段 2：抽出统一世界查询

- 增加 `WorldQuery`。
- 将 `known_locations` 的生成移动到查询层。
- 先保持地点候选数量接近现有行为。
- 处理权限、可达性和固定目标地点保留规则。

验收：地点 ID、权限和行动可行性测试通过。

### 阶段 3：拆分任务上下文

- 增加 `ContextService`。
- 为行动、每日计划、生活计划、候选选择、对话和记忆形成分别定义视图。
- 将完整 `mental_state` 改为主动目标、当前步骤和相关认知摘要。
- 保留来源 ID 和诊断信息。

验收：输入规模下降，同时计划、行动和对话回归测试通过。

### 阶段 4：增加相关检索

- 扩展 `MemoryRetriever`。
- 增加按意图、目标、参与者、地点和任务类型的查询。
- 增加相关关系和共同记忆查询。
- 明确“无结果”和“无权限”的区别。

验收：记忆驱动对话、事实引用和隐私过滤测试通过。

### 阶段 5：增加持久认知

- 评估 goals、intentions、beliefs 和 habits 的跨重启要求。
- 新增对应数据库表或持久事件记录。
- 在启动时恢复有效记录，在 reset 时按规则清理。

验收：跨日和重启恢复测试通过。

### 阶段 6：增加版本化提交

- 为 Agent 和世界状态增加 `state_version`。
- 所有异步 LLM 请求绑定版本和模拟时间。
- 处理 stale 响应。
- 增加并发和延迟场景测试。

验收：旧计划、旧地点、资源变化和 reset 后的异步结果都不能错误提交。

## 16. 测试要求

### 数据边界

- 即时天气不污染长期状态。
- 天气导致的迟到或情绪变化能追溯到天气事实。
- 记忆和关系可以跨 tick 使用。
- 需要持久化的习惯、意图和信念可以恢复。

### 上下文检索

- 不发送无关地点。
- 私人地点不会出现在无权限角色的可执行候选中。
- 不发送未来事实。
- 不把无关角色的关系和记忆带入当前请求。
- 当前固定承诺不会被裁剪掉。
- 紧急需求优先级高于普通 routine block。
- 检索为空时不会诱导 LLM 进行确定性臆测。

### 异步一致性

- LLM 返回前意图失效时，结果不会执行。
- 资源在请求期间耗尽时，结果会被重新校验。
- Agent reset 后旧任务不会修改新状态。
- 多个后台请求完成顺序变化时，最终状态仍然合法。

### 性能与观测

- 记录各任务平均输入规模。
- 记录 P50、P95 请求耗时。
- 记录候选检索耗时和数据库查询次数。
- 记录 stale、fallback、重试和上下文为空的次数。
- 验证 SQLite 不出现明显的并发锁等待。

## 17. 尚需确认的产品决策

以下问题需要在实现前确定：

1. 应用重启后是否需要继续当天模拟，还是只重新开始模拟但保留长期记忆？
2. goals、intentions、beliefs 是否都需要跨重启保存？
3. habits 是否属于长期行为依据？如果是，应持久化。
4. 是否需要完整天气和资源历史回放？
5. 上下文预算按字符、token，还是按模型实际 tokenizer 计算？
6. LLM 请求过期后是丢弃、重新请求，还是使用确定性 fallback？
7. 关系缓存是否需要，还是每次直接从 `RelationshipStore` 读取？
8. 是否需要支持多个 WorldPack 同时运行？

## 18. 最终目标

最终系统应具备以下特征：

```text
完整状态始终保存

当前任务触发情境分析

内存提供即时世界状态
数据库提供长期行为依据

ContextService 只激活相关信息

LLM 只在代码定义的合法范围内选择

返回结果经过时间、权限、可行性和版本校验

行动结果再更新完整状态和持久记录
```

这样既能保留角色的连续性，又不会让每次 LLM 请求都携带全部记忆、全部地点和全部心智字段。
