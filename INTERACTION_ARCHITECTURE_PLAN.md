# 交互驱动的 Mini-Town 架构改造方案

## 1. 背景与目标

产品需要展现出持续的活力，但活力不应主要依赖随机动画或大量无意义事件，而应来自可追踪的因果链：

```text
角色的行为改变世界
世界的变化影响角色
角色之间的互动改变彼此后续行为
```

目标是让角色与角色、角色与世界、世界与角色之间形成真实的交互循环，同时保证：

- 完整状态不会因为上下文裁剪而丢失
- LLM 每次只处理当前任务需要的信息
- 世界状态、权限、资源和事实由代码控制
- 交互结果可以影响后续目标、记忆、关系和行动
- 用户可以理解事件为什么发生以及产生了什么影响
- 仿真在 LLM 延迟、失败或返回过期结果时仍能继续运行

## 2. 当前项目现状

当前主要模块：

- `SimulationEngine`：推进时间、调度 Agent、提交动作和广播状态
- `Agent`：角色状态机、移动、活动、感知和行动执行
- `CognitivePlanner`：每日计划、生活计划和行动决策
- `InteractionEngine`：动作可行性验证及确定性效果
- `TownAgent`：天气、基础设施和随机事件
- `DialogueManager`：角色之间的持续对话
- `MemoryStore`：持久记忆和记忆检索
- `RelationshipStore`：角色关系持久化
- `FactLedger`：当前运行中的事实账本
- `HabitStore`：当前运行中的行为倾向

目前已经具备以下基础：

- 角色和世界行动由代码验证后才提交
- 资源、设施、服务和物流具有结构化状态
- 对话、记忆、关系和事实有独立模块
- 天气等环境状态在内存中维护
- 高频 LLM 调用默认使用 chat 模式
- 低频的反思、记忆心理解释和对话评估仍使用 thinking 模式

当前上下文构建仍存在较大范围传递的问题：

- `mental_state` 会整体传入规划请求
- `known_locations` 会把所有地点传入每个角色的请求
- 事实、目标、意图和历史结果尚未完全按任务筛选
- 不同任务的上下文拼接逻辑分散在不同模块
- LLM 请求和状态提交之间缺少统一的版本校验机制

默认世界当前约有 6 个居民、17 个地点、34 个资源/实体和 1 个过程。单个 Agent 的规划上下文实测约为 8,000 至 9,500 个字符，其中 `mental_state` 和 `known_locations` 占主要部分。

## 3. 核心数据分层

### 3.1 内存：当前运行状态

内存保存只代表当前仿真运行状态、变化频繁且不一定需要跨重启保留的数据：

- 当前模拟时间
- 当前天气
- 当前基础设施即时状态
- Agent 位置、状态和当前动作
- Agent 当前需求
- Agent 当前情绪
- 当前执行中的计划步骤
- 当前事件队列和刺激队列
- 当前世界资源的运行时数量

这些数据需要快速读取和更新，不应因为每次请求都访问数据库而增加仿真热路径的延迟。

### 3.2 数据库：跨时间有效的行为依据

数据库保存会影响未来行为、需要追溯或需要跨 tick 保留的数据：

- 记忆
- 关系
- 对话及对话消息
- 重要事实或事件历史
- 长期行为倾向
- 需要跨重启恢复的意图、信念和目标
- 需要审计的交互记录

完整数据仍然保存，但不代表每次 LLM 请求都读取和发送完整数据。

### 3.3 静态世界目录

地点、角色初始资料、世界规则和流程定义属于声明式世界内容：

- 地点目录
- 地图和通行规则
- 角色初始资料
- 资源和设施定义
- 过程定义
- 天气状态转移规则
- 节日和公共事件模板

这类数据只维护一份权威副本，可以在内存中只读缓存。每次请求前根据角色权限和当前任务查询所需的子集。

### 3.4 当前状态与历史记录的边界

瞬时状态可以只保存在内存，但它造成的行为结果需要持久化：

```text
当前天气：内存
角色因大雨迟到：事实/记忆
角色以后因为大雨调整出行：意图/行为倾向
```

同理：

```text
当前库存：内存
库存不足导致补货任务：持久事件/任务
当前设施是否开放：内存
设施故障导致角色行动失败：事实/记忆
```

## 4. 交互模型

交互应成为系统中的一等数据，而不是分散在 action、event、fact、memory 和 relationship 中。

### 4.1 交互记录

建议引入统一的 `InteractionRecord`，描述一次交互尝试：

```python
InteractionRecord(
    id,
    sim_timestamp,
    state_version,
    initiator_type,
    initiator_id,
    target_type,
    target_id,
    interaction_type,
    intention_id,
    payload,
    visibility,
    status,
)
```

字段语义：

- `initiator`：谁发起，例如 Agent 或 world system
- `target`：目标可以是 Agent、地点、设施、资源或世界系统
- `interaction_type`：移动、对话、请求服务、观察、生产、运输等
- `intention_id`：这次交互服务于哪个意图
- `state_version`：构建请求时的状态版本
- `visibility`：哪些角色可以知道这次交互
- `status`：proposed、validated、executing、completed、blocked、expired

### 4.2 交互结果

建议引入统一的 `InteractionOutcome`：

```python
InteractionOutcome(
    interaction_id,
    status,
    effects,
    fact_ids,
    memory_candidates,
    relationship_changes,
    follow_up_opportunities,
)
```

一次交互可以同时产生多种效果：

- 世界状态效果
- Agent 状态效果
- 需求和情绪变化
- 事实
- 记忆候选
- 关系变化
- 任务进度
- 后续意图或机会
- 前端可展示事件

### 4.3 三类交互

#### 角色与角色

包括：

- 打招呼和聊天
- 请求帮助和提供服务
- 共同完成任务
- 争论、拒绝和误解
- 形成承诺
- 由于过去互动而主动寻找或回避某人

#### 角色与世界

包括：

- 移动和观察
- 取用、购买、生产和运输资源
- 使用设施和服务
- 改变库存、设备、任务和地点状态
- 受到天气、营业时间、容量和路径限制

#### 世界与角色

包括：

- 天气和基础设施变化
- 公共事件和节日
- 新任务和新机会
- 地点关闭或重新开放
- 资源供应变化
- 对角色已有目标造成的新约束

## 5. 世界系统 Agent

### 5.1 定位

世界可以被建模为一个系统 Agent，但不应与人类 Agent 完全相同。

世界系统拥有：

- 当前环境状态
- 系统规则
- 公共目标
- 触发条件
- 可执行系统动作
- 对角色产生的刺激、机会和约束

世界系统不应直接替人类 Agent 决定行动。

### 5.2 第一阶段不使用 LLM

天气变化、库存阈值、道路可达性、设施状态和任务创建等优先使用确定性规则：

- 容易测试
- 容易复现
- 不会产生虚假事实
- 不会因 LLM 延迟阻塞世界
- 能明确计算前置条件和效果

LLM 后续可以用于：

- 公共事件的自然语言描述
- 世界状态变化的摘要
- 复杂系统策略的候选方案
- 面向用户的事件叙述

LLM 不负责：

- 判断天气是否变化
- 判断资源是否存在
- 判断道路是否可达
- 判断设施是否真的修复
- 判断角色是否完成任务

### 5.3 世界刺激、机会和约束

建议把世界变化统一转换成三种对象：

```python
WorldStimulus(
    type,
    location,
    target_agents,
    public,
    facts,
    urgency,
)
```

```python
WorldOpportunity(
    type,
    location,
    eligible_agents,
    reward,
    deadline,
    prerequisites,
)
```

```python
WorldConstraint(
    type,
    affected_locations,
    affected_agents,
    effects,
    expires_at,
)
```

示例：

```text
餐厅库存低于阈值
→ 世界系统创建补货机会
→ 店主和符合条件的运输者获得刺激
→ 角色决定是否接受
→ 运输交互改变资源状态
→ 餐厅营业状态发生变化
```

## 6. 感知与上下文激活

### 6.1 感知队列

世界事件不能直接发送给所有角色。应先按规则筛选可感知者：

- 天气变化：所有角色可以感知
- 餐厅库存不足：店主一定可以感知，现场角色可能可以感知
- 某人拒绝帮助：参与者知道，现场旁观者可能知道
- 私人地点内发生的事情：只有符合权限和观察条件的角色知道

每个 Agent 可以拥有待处理刺激队列：

```python
agent.stimuli = [
    stimulus_1,
    stimulus_2,
]
```

刺激需要有优先级、来源、时间和是否已处理状态，避免同一刺激在每次决策中重复激活。

### 6.2 不使用单一“当前意图”

上下文不能只根据一个当前意图构建。需要综合：

```text
固定承诺
+ 当前计划步骤
+ 紧急需求
+ 开放意图
+ 当前环境刺激
+ 附近角色和关系
+ 近期行动结果
```

建议先由代码计算一组 active considerations，再构建任务上下文。

### 6.3 任务专用上下文

完整状态继续保存，但每种 LLM 任务只获取必要字段。

行动决策上下文：

```python
ActionContext(
    current_time,
    current_location,
    needs,
    current_commitment,
    current_plan_step,
    urgent_stimuli,
    relevant_intentions,
    relevant_memories,
    nearby_agents,
    accessible_locations,
    available_actions,
)
```

对话上下文：

```python
DialogueContext(
    participants,
    current_location,
    observable_facts,
    shared_memories,
    relationship,
    recent_dialogue,
    relevant_open_intentions,
)
```

反思上下文：

```python
ReflectionContext(
    time_window,
    important_experiences,
    unresolved_events,
    recent_outcomes,
)
```

### 6.4 地点查询

地点目录只维护一份，但请求前查询当前角色相关的地点：

```python
world_query.locations_for(
    agent=agent,
    intention=current_intention,
    snapshot=snapshot,
    limit=6,
)
```

查询应考虑：

- 进入权限
- 当前地点
- 距离和预计移动时间
- 当前任务或意图
- 地点是否开放
- 天气和路径成本
- 地点内是否有相关资源、角色或服务

不能仅通过 prompt 让 LLM 自己判断权限和可达性。

## 7. 状态一致性与版本控制

### 7.1 统一快照

数据库和内存虽然保存不同类型的数据，但上下文构建仍需要有明确的模拟时间边界。

建议构造：

```python
DecisionSnapshot(
    sim_timestamp,
    state_version,
    world_state,
    agent_state,
    relevant_persistent_data,
)
```

数据库查询必须满足：

```text
记录时间 <= snapshot.sim_timestamp
```

内存中的状态也必须来自同一个模拟时间或版本。

### 7.2 过期结果校验

LLM 请求返回时重新检查：

- Agent 是否仍然处于允许执行的状态
- 当前意图是否仍然有效
- 当前计划步骤是否已完成或阻塞
- 角色是否仍在预期地点
- 资源是否仍然可用
- 当前 `state_version` 是否匹配
- 当前时间是否已经超过行动窗口

过期结果不能直接提交，应丢弃、重新验证或重新请求。

## 8. 交互提交流程

所有交互采用“提议、验证、提交”三阶段：

```text
LLM 或规则系统提出交互
    ↓
InteractionEngine 验证权限、前置条件和可行性
    ↓
生成 InteractionRecord
    ↓
提交确定性世界效果
    ↓
记录 InteractionOutcome
    ↓
更新事实、记忆、关系、任务和意图
    ↓
生成新的刺激或机会
    ↓
广播前端事件
```

LLM 只能提出：

```json
{
  "interaction_type": "move",
  "location": "restaurant",
  "intention_id": "intent_xxx"
}
```

LLM 不能直接修改资源、库存、关系或事实。

## 9. 交互结果如何产生活力

### 9.1 成功示例

```text
小美向咖啡馆请求饮品
→ 服务台和库存校验通过
→ 饮品进入小美背包
→ 咖啡馆库存减少
→ 小美形成消费经历
→ 小美与店员产生服务互动记录
→ 小美可能继续停留
→ 双方可能开始对话
```

### 9.2 失败示例

```text
小美请求饮品
→ 服务台无人值守
→ 请求失败事实
→ 小美保留未完成意图
→ 情绪或关系产生轻微变化
→ 小美重新选择其他地点或改为回家
```

### 9.3 世界事件示例

```text
大雨
→ 公园移动成本增加
→ 角色重新评估是否去公园
→ 有人取消见面
→ 另一角色未能遇见对方
→ 关系和后续对话发生变化
```

重点不是事件数量，而是事件能否影响后续行为。

## 10. 记忆、关系与事实

### 10.1 记忆

记忆只记录有行为价值或情绪价值的经历，不记录所有 tick。

记忆检索应综合：

- 语义相关性
- 时间新鲜度
- 重要性
- 是否未解决
- 是否涉及当前人物
- 是否涉及当前地点
- 是否关联当前目标或意图

“没有检索到”必须与“确定不存在”区分开。

### 10.2 关系

关系可以持久化到数据库，也可以在内存中保留当前运行缓存。需要明确：

- 数据库是权威来源
- 缓存何时刷新
- 对话、服务、帮助、拒绝和共同任务如何改变关系
- 关系变化是否需要来源交互 ID

### 10.3 事实

事实用于约束角色对确定信息的引用。每条事实应包含：

- 发生时间
- 产生者
- 地点
- 参与者
- 可知角色
- 来源交互
- 事实内容

角色不能引用自己不可知、未来发生或没有来源的事实。

## 11. 前端表现

前端不应只展示孤立事件，而应展示交互的原因和结果：

```json
{
  "interactionId": "interaction_xxx",
  "cause": "restaurant_inventory_low",
  "parentEventId": "event_xxx",
  "outcome": "补货任务已创建",
  "affectedAgents": ["liu", "wang"]
}
```

用户应该能看到类似的事件链：

```text
餐厅库存不足
→ 大刘发布补货请求
→ 老王接受运输任务
→ 运输途中遇到大雨
→ 预计晚到 15 分钟
```

地图、事件流、角色详情和对话详情应使用同一组 interaction ID 和 fact ID，避免不同页面显示互不关联的信息。

## 12. 防止系统失控

需要设置：

- 每 tick 最大交互数量
- 同类事件冷却时间
- 交互链最大深度
- 重复事件合并规则
- 单个 Agent 的刺激队列上限
- 事件优先级
- LLM 并发上限
- 过期请求清理机制

世界事件只有在至少影响以下一项时才值得生成：

- 资源
- 地点可用性
- 角色目标
- 任务
- 关系
- 记忆
- 后续机会

否则应视为背景噪音，不进入主要事件流。

## 13. 观测与评估指标

需要记录上下文元数据，但不建议默认记录完整 prompt：

```text
task_type
selected_memory_ids
selected_fact_ids
selected_location_ids
omitted_sections
estimated_input_tokens
sim_timestamp
state_version
interaction_id
```

评估指标包括：

- 每个模拟小时的有效交互数
- 产生后续行为的交互比例
- 角色对之间的互动多样性
- 世界事件导致角色行为变化的比例
- 重复行动比例
- 交互失败后的重新规划比例
- 过期 LLM 结果丢弃比例
- 平均输入 token 数
- 平均 LLM 延迟
- 上下文检索漏掉关键事实的比例
- 记忆触发后实际产生后续行为的比例

不以“事件越多越好”为目标。

## 14. 实施阶段

### 阶段一：统一交互记录

- [ ] 定义 `InteractionRecord`
- [ ] 定义 `InteractionOutcome`
- [ ] 为移动、活动、资源操作、服务和对话生成交互记录
- [ ] 给现有事件补充 `interactionId`
- [ ] 保留已有事件兼容字段

### 阶段二：世界系统刺激与机会

- [ ] 将 TownAgent 输出转换为世界刺激、机会和约束
- [ ] 增加按角色筛选可感知者的规则
- [ ] 增加 Agent 刺激队列
- [ ] 为库存、设施和物流补充可产生后续行为的规则
- [ ] 保持第一阶段完全确定性，不引入世界 LLM

### 阶段三：统一上下文服务

- [ ] 定义 `ContextService`
- [ ] 将完整心智状态与任务上下文分离
- [ ] 增加行动、对话、每日计划和反思的独立上下文视图
- [ ] 只查询当前角色相关地点
- [ ] 按权限、时间和相关性筛选记忆与事实
- [ ] 增加上下文 token 预算和裁剪顺序

### 阶段四：状态快照与过期校验

- [ ] 定义模拟时间和 `state_version`
- [ ] 上下文请求绑定 Agent、意图、计划步骤和版本
- [ ] LLM 返回后重新验证版本和执行条件
- [ ] 处理过期、取消和重复请求
- [ ] 测试实时模式下的延迟返回

### 阶段五：持久化边界

- [ ] 确认 goals、intentions、beliefs 是否跨重启保留
- [ ] 确认 habits 是否跨重启保留
- [ ] 确认 daily plan 是否需要恢复
- [ ] 为需要持久化的状态增加表或事件重建机制
- [ ] 保证内存缓存和数据库权威记录的更新顺序

### 阶段六：因果链与前端展示

- [ ] 事件包含交互 ID、来源 ID和父事件 ID
- [ ] 事件流展示原因和结果
- [ ] 地图动作与事件流关联
- [ ] 角色详情展示相关交互、记忆和关系变化
- [ ] 增加交互链详情或事件展开视图

### 阶段七：回归测试和指标

- [ ] 单个角色与世界的成功交互
- [ ] 单个角色与世界的失败交互
- [ ] 两个角色的对话和关系变化
- [ ] 世界事件触发角色重新决策
- [ ] 交互产生记忆并在后续被激活
- [ ] 旧结果在状态版本变化后不被执行
- [ ] 权限和信息可见性测试
- [ ] 多日仿真测试
- [ ] 测量输入 token、延迟和有效交互比例

## 15. 验收标准

### 数据正确性

- 完整心智状态和世界状态不会因上下文裁剪而丢失
- 同一类数据只有一个权威来源
- 内存和数据库之间没有未定义的双向独立修改
- 未来事实不会进入角色当前上下文

### 行为正确性

- LLM 不能直接绕过代码修改世界
- 不可达地点、无权限地点和不可用资源不会被执行
- 失败交互会产生可处理的结果，而不是静默消失
- 过期决策不会直接提交

### 活力表现

- 世界事件能够产生新的角色选择
- 角色交互能够改变后续关系或意图
- 角色行为能够改变世界资源、任务或设施状态
- 重要交互能够形成后续记忆或行为
- 事件流能够展示至少一层原因和结果关系

### 性能表现

- 普通高频行动请求使用 chat 模式
- thinking 模式只用于低频、高价值任务
- 任务上下文不会默认携带完整世界目录和完整心智历史
- 上下文大小、检索结果和 LLM 延迟可观测
- LLM 失败时仿真仍可以继续运行

## 16. 设计原则总结

```text
完整状态始终保存
上下文按任务激活
世界由规则控制
LLM 只提出有限范围内的选择
代码验证权限、事实和效果
交互结果驱动后续状态
前端展示因果链而不是随机噪音
```

最终目标不是让系统看起来更忙，而是让用户感受到：

```text
这个角色刚刚做的事情，会在之后留下痕迹，并改变这个世界。
```
